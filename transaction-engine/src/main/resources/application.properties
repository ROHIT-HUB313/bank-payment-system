server.port=8083
spring.application.name=transaction-engine

# Database - PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/bank_transaction_db
spring.datasource.username=bank_app
spring.datasource.password=App@SecurePass
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# Valid Hostname for Eureka
eureka.instance.hostname=localhost
